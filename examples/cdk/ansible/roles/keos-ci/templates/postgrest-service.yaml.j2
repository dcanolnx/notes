apiVersion: v1
kind: Service
metadata:
  name: postgrest
  namespace: "{{ jenkins_namespace }}"
  labels:
    app: postgrest
spec:
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: postgrest
