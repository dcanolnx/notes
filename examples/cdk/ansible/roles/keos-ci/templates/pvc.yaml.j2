apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ pvc_name }}
  namespace: "{{ pvc_namespace }}"
spec:
  accessModes:
    - {{ pvc_access_mode }}
  resources:
    requests:
      storage: {{ pvc_size }}
  storageClassName: {{ pvc_storage_class }}
