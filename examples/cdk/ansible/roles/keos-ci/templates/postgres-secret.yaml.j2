apiVersion: v1
kind: Secret
metadata:
  namespace: "{{ jenkins_namespace }}"
  name: postgres
type: Opaque
data:
  POSTGRES_USER: {{ postgres_username | b64encode }}
  POSTGRES_PASSWORD: "{{ postgres_password | b64encode }}"
